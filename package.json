{
  "name": "autonomous-realms",
  "version": "1.0.0",
  "description": "Autonomous Realms - AI-powered isometric world",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd client && npm install && npm run preview"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
